<h1>Tutor Matching Page</h1>
<% 
=begin %>
<%= form method = "POST" action="@form_step_2">
    <select name="" id="">
        <%= for tutors do |matchingTutor|%>
            <option value="<%=matchingTutor.id%>"><%=matchingTutor.name%> </option>
        <%end%>
    </select>

<%
=end %>

<%=form_tag('index', :method => "get", :controller => 'tutor' ) do %>
    <%= label_tag(:filter_major, "Course Department: ") %> 
    <br>
        <%= text_field_tag(:filter_major) %>
    <br>
        <%= submit_tag("Find") %> 
<%end%>

<% if @matchingTutors.present? == false %>
    <p> 
        <%= @noTutors %>
        <br>
        <%= @askRequest %> 
        <%=form_tag('request_submission', :method => "get", :controller => 'tutor' ) do %>
            <%= label_tag(:requestedCourse, "Requested Course Department: ") %> 
            <br>
                <%= text_field_tag(:requestedCourse) %>
            <br>
                <%= submit_tag("Submit") %> 
        <%end%>

    </p>
<% else %>
    <ul>
    <% @matchingTutors.each do |user| %>
        <% if user.major.nil? == false %>
        <!-- makes sure users with no majors are not displayed -->
            <li>
                <%= link_to user.first_name.to_s + ' ' + user.last_name.to_s + ' Major: ' + user.major.to_s, user %>
            </li>
            <% end %>
        <% end %>
    </ul>    

<%end%>